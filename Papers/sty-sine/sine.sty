% 定义样式文件
% \ProvidesPackage{sine}
% 基本信息
\def\semester{Spring 2025}
\def\instructor{2023111228 Haoyi Zhang}
% ============ 基础包加载 ============

\RequirePackage{hyperref}
\RequirePackage{geometry}
\RequirePackage{fancyhdr}
\RequirePackage{lastpage}

\RequirePackage{amssymb,amsfonts}
\RequirePackage{amsmath,amsthm}
\RequirePackage{xparse,physics,tikz}
\RequirePackage{thmtools,algorithm,algorithmicx,algpseudocode}
\RequirePackage{booktabs,tabularx,array,multirow,longtable,makecell}
\RequirePackage{lscape}
\RequirePackage{mdframed}
\RequirePackage{ctex}

% ============ 选项配置 ============

\newif\ifsomeflag % 可选：定义一些内部开关
\DeclareOption{cn}{
    \RequirePackage{ctex}
}
\ProcessOptions\relax

% ============ 图片配置 ============
\RequirePackage{graphicx,caption,float}


% ============ 颜色配置 ============
\usepackage{sty-sine/sine-Colors}

% ============ 超链接配置 ============
\hypersetup{
    colorlinks=true,
    linkcolor=SufeRed,
    citecolor=blue,
    filecolor=magenta,
    urlcolor=SineBlue,
}

% ============ 字体配置 ============
\RequirePackage{fontspec}  % 显式加载 fontspec

% 衬线体: Garamond
\setmainfont{Adobe Garamond Pro}

% \setsansfont{LXGW WenKai}
\RequirePackage[ebgaramond]{newtxmath}
\setmonofont{LXGW WenKai Mono}
% 无衬线体: LXGW WenKai 适合屏幕阅读
% \setCJKsansfont{LXGW WenKai}
\setCJKmonofont{LXGW WenKai Mono}
% \RequirePackage{unicode-math}
% \setmathfont{Noto Sans Math}

% ============ 页面布局 ============
% \geometry{
%   a4paper,
%   top=1in,
%   bottom=1in,
%   left=0.75in,
%   right=0.75in
% }

\RequirePackage{graphicx}
\RequirePackage{graphicx}       % 图片支持
\RequirePackage{subcaption}     % 子图支持

% ============ 页眉页脚 ============
% \fancypagestyle{IEEEtran}{
%   \fancyhf{}
%   \fancyhead[L]{\footnotesize{\class, \semester}}
%   \fancyhead[C]{\footnotesize{\shorttitle}}
%   \fancyhead[R]{\footnotesize{\instructor}}
%   \fancyfoot[R]{\footnotesize{\thepage} of \footnotesize{\pageref*{LastPage}}}
% }
% \pagestyle{IEEEtran}


% =========== 数理统计 ===========
\usepackage{sty-sine/Statistics}

% =========== 财务会计 ============
% \usepackage{sty-sine/Accounting}

% ============ 定理环境配置 ============

\declaretheoremstyle[
  % headfont=\color{red}\bfseries,  % 标题：红色、加粗
  bodyfont=\color{kleinblue}   % 正文：蓝色、斜体
]{kleinbody}

\declaretheoremstyle[
  % headfont=\color{red}\bfseries,  % 标题：红色、加粗
  bodyfont=\color{black}   % 正文：蓝色、斜体
]{normal}

% \declaretheorem[thmbox=M, name=Theorem, numberwithin=section]{theorem}
% \declaretheorem[shaded={},name=Definition]{definition}
% \declaretheorem[shaded={},name=Observation]{ob}
% \declaretheorem[thmbox=M, name=Property, sibling=definition]{property}
% \declaretheorem[thmbox=M, name=Lemma, numberwithin=section]{lemma}
% \declaretheorem[thmbox=M, name=Corollary, numberwithin=section]{corollary}
% \declaretheorem[thmbox=M, name=Proposition, numberwithin=section]{proposition}
\declaretheorem[style=remark, thmbox=M, name=Remark]{remark}
% \declaretheorem[thmbox=S, name=Example, numberwithin=section]{example}
% \declaretheorem[name=Proof, numbered=no, qed=\qedsymbol]{Myproof}
\declaretheorem[shaded={}, name=Question]{question}
% \declaretheorem[name=Solution, qed=\qedsymbol,style=kleinbody,numbered=no]{solution}
% % ============ 其他配置 ============
% \allowdisplaybreaks % 允许公式跨页断行
